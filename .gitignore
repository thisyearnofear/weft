# Dependencies
node_modules/
.pnp/
.pnp.js

# Build
dist/
build/
*.log

# First-party analytics events (written by frontend /api/track)
/data/

# Environment
.env
.env.local
.env.*.local
.venv-signoz/
agent/.env

# IDE / agent tooling
.cursor/
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Testing
coverage/
.nyc_output/
.pytest_cache/

# Private multi-host ops playbook (SSH aliases, IPs, disk policy)
OPS.local.md
ops.local.md

# Private pilot outreach (org contacts — do not commit)
canton/PILOT_TARGETS.local.md

# Weft agent artifacts
agent/.cache/
agent/.attestations/
agent/.inbox/
agent/.axl/
agent/.downloads/
__pycache__/
*.pyc
*.pyo

# Smart Contracts
contracts/cache/
contracts/out/
contracts/out-fhe/
contracts/cache-fhe/
artifacts/

# Soldeer dependencies (FHEVM) — soldeer.lock ignored; casting.yaml.lock is committed for SigNoz judge reruns
dependencies/
soldeer.lock

# SigNoz OpenTofu/Terraform local state (signoz/terraform/*.tf + .terraform.lock.hcl are committed)
signoz/terraform/.terraform/
signoz/terraform/terraform.tfstate
signoz/terraform/terraform.tfstate.backup
signoz/terraform/*.tfvars

# Node
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Hardhat
deployments/

# Deploy outputs
scripts/deployed-addresses.json
scripts/.env
broadcast/
cache/
out/

.junie
.blackboxcli/
.output.txt
.claude/

# Agent skill installs and local dev tooling state
.agents/
.devin/
skills-lock.json

# Backup files
*.bak

# Demo recordings (keep locally, never commit binaries / presigned URLs)
weft-demo-recording.mp4
weft-demo-project.mp4
weft-demo-recording-url.txt
assets/demo-recording.webm
assets/weft-demo-hackathon.mp4
*.mp4
videos/

# HyperFrames video render output (generated, not source)
compositions/**/renders/

# Submission screenshots (large binary assets; keep locally, upload to portal)
docs/submission-screenshots/

# TestSprite run artifacts (screenshots/video/snapshots from individual runs —
# keep the test definitions in .testsprite/plans/ and .testsprite/backend/)
.testsprite/failure/
.testsprite/failure-*/
pours/
