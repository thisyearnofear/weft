# Ethereum Mainnet RPC (for ENS resolution - server-side, no CORS)
MAINNET_RPC=https://ethereum-rpc.publicnode.com

# Weft Contract Addresses
NEXT_PUBLIC_DEFAULT_CHAIN=sepolia

# Sepolia
NEXT_PUBLIC_WEFT_MILESTONE_SEPOLIA=
NEXT_PUBLIC_VERIFIER_REGISTRY_SEPOLIA=
# Zama FHEVM confidential milestones (Sepolia only)
NEXT_PUBLIC_WEFT_MILESTONE_CONFIDENTIAL_SEPOLIA=

# Base Sepolia
NEXT_PUBLIC_WEFT_MILESTONE_BASE_SEPOLIA=
NEXT_PUBLIC_VERIFIER_REGISTRY_BASE_SEPOLIA=

# Mainnet
NEXT_PUBLIC_WEFT_MILESTONE_MAINNET=
NEXT_PUBLIC_VERIFIER_REGISTRY_MAINNET=

# Base
NEXT_PUBLIC_WEFT_MILESTONE_BASE=
NEXT_PUBLIC_VERIFIER_REGISTRY_BASE=

# WalletConnect (get from https://cloud.walletconnect.com)
NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID=

# Upstream status API (server-side proxy target).
# Local dev: the default below works when weft_status_api.py runs on :9010.
# Vercel/production: MUST be the public nginx endpoint — serverless functions
# cannot reach the VPS's localhost.
WEFT_STATUS_API_URL=http://127.0.0.1:9010
# WEFT_STATUS_API_URL=https://api.weft.persidian.com   # production

# Shared secret the snel-bot nginx proxy requires on every upstream request.
# Sent automatically as `x-weft-key` on calls to WEFT_STATUS_API_URL /
# CANTON_API_URL (see src/lib/upstreamAuth.ts). Server-side only — never
# expose with a NEXT_PUBLIC_ prefix.
# WEFT_STATUS_API_KEY=

# Canton API on nuncio (optional — /canton pages degrade gracefully without it)
# CANTON_API_URL=https://canton-api.example.com

# 0G Galileo (server-side — milestone metadata API route)
# ZERO_G_INDEXER_RPC=https://indexer-storage-testnet-turbo.0g.ai
# ETH_RPC_URL=https://evmrpc-testnet.0g.ai
# NEXT_PUBLIC_WEFT_MILESTONE_0G_TESTNET=0x9f66158c560ce5c8b40820fdcd2874ff8d852192

# fal.ai — AI-woven textile swatch generation for milestone chronicle cards
# Get a key at https://fal.ai — used by the daemon to generate unique milestone images
NEXT_PUBLIC_FAL_IMAGE_URL=https://queue.fal.run