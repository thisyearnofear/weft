Agents decide. KeeperHub executes.

Milestone escrow needs more than a verdict. It needs reliable onchain execution. Most verifier agents stop at the decision — who actually submits the transaction, handles gas, retries on failure, and lands a confirmed receipt?

Weft is an autonomous milestone verifier on zero G. It collects deterministic evidence, reaches peer consensus, then hands the last mile to KeeperHub over MCP.

Here's real proof on zero G Galileo, chain sixteen six zero two. Our agent called KeeperHub's execute contract call tool, ran stake on the live WeftMilestone contract, and got a confirmed transaction onchain. Auditable execution ID, idempotency key, explorer link — all written to keeperhub audit JSON beside the attestation bundle.

The flow is simple. Weft agent gathers evidence and decides. KeeperHub MCP receives the call. KeeperHub broadcasts reliably. Zero G chain records the stake.

Agent thinks. KeeperHub acts. Capital moves onchain.

Weft — built for Agents Onchain. Try it at weft dot thisyearnofear dot com.
